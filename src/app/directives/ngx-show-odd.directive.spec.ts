import { NgxShowOddDirective } from './ngx-show-odd.directive';

describe('NgxShowOddDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxShowOddDirective();
    expect(directive).toBeTruthy();
  });
});
